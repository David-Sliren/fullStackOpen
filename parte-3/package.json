{
  "name": "parte-3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -r dist && cd ../parte-2/phoneBook && rm -r dist && pnpm run build && cp -r dist ../../parte-3 && echo 'se hizo'",
    "deploy": "fly deploy",
    "deploy:full": "pnpm run build:ui && pnpm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "nodemon": "^3.1.14"
  }
}
